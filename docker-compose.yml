version: '3.9'
services:
  backend:
    build: ./backend
    environment:
      - MOCK_LLM=1
    ports:
      - "8000:8000"
  frontend:
    build: ./frontend
    environment:
      - VITE_BACKEND_URL=http://localhost:8000
    ports:
      - "5173:5173"
    depends_on:
      - backend
